# Engineering Stack

The main backend is Python and Django with PostgreSQL. Staff use Django admin for operational workflows. Public and internal integrations are exposed through REST APIs. Background work is handled by Celery and Redis. Services run on Linux and are deployed in containers. Git is used for source control and code review.
